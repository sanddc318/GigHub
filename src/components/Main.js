import React, { Component, Fragment } from 'react'

import Form from './Form'

const Main = () => (
  <main className="master-container">
    <section className="container container--left">
      <Form />
    </section>

    <section className="container">Placeholder</section>
  </main>
)
export default Main
